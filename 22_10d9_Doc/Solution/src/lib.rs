/// Adds two integers together.
///
/// # Arguments
/// * `a` - The first integer.
/// * `b` - The second integer.
///
/// # Returns
/// The sum of `a` and `b`.
pub fn add(left: u64, right: u64) -> u64 {
    left + right
}